Ce am implementat acum
CSV orders import (deja live)

POST /api/v1/orders/import-csv (multipart, field file)
Creează sales_orders + sales_order_lines; dacă lipsește order_number, generează automat CMD_#####; face upsert minim pentru SKU-uri noi.
PDF “Nota de culegere”

GET /api/v1/orders/:id/pick-note.pdf
Layout cu antet, partener/adresă/contact, tabel cu:
Denumire articol, Cod gest., Lungime din care se taie, Cantitate de, UM, Lot intrare, Cantitate rămasă, Greutate, Lungimi solicitate
Total greutate la final, plus Tip livrare și Agent.
Implementat cu pdfkit; stream către browser.
Structură DB pentru comenzi

017_create_sales_orders.sql — tabele pentru comenzi și linii + secvență CMD.
Comanda exemplu din poze a fost importată cu succes:
number: CMD_108818, total_weight: 701.11
Rute noi în inventory service

GET /api/v1/orders (listare cu paginație)
GET /api/v1/orders/:id (detalii)
GET /api/v1/orders/:id/pick-note.pdf (PDF)
POST /api/v1/orders/import-csv (upload CSV)
Cum testezi rapid
Import CSV (exemplu deja în repo):
ORDER_IMPORT_EXAMPLE.csv
În PowerShell:
Listă comenzi:
PDF pick-note:
Notițe de implementare
Coloanele “Lungime din care se taie” și “Cantitate rămasă” sunt momentan goale în PDF (le vom popula automat după ce implementăm pasul de alocare + CUT).
“Lungimi solicitate” se formatează din requested_lengths (ex.: “50+20+30”).
Greutatea liniilor se preia din CSV (dacă există) pentru total.
Ce urmează (propunere)
Alocare/rezervări (MIN_WASTE/FIFO) și popularea automată a “din care se taie” + “rămasă”.
UI în Web UI:
Pagină “Comenzi” cu:
buton “Import CSV” (drag&drop),
listă comenzi + acțiune “Printează Nota de culegere”.
Retuș PDF:
adăugare logo, borduri mai groase, lățimi dinamice; opțional semnătură preluare și spațiu observații.
Vrei să continui direct cu:

buton “Import CSV” + listă comenzi + “Printă PDF” în frontend
sau

logica de alocare automată (și completare câmpuri “din care se taie” / “rămasă”)?## Notițe de implementare

Coloanele “Lungime din care se taie” și “Cantitate rămasă” sunt momentan goale în PDF (le vom popula automat după ce implementăm pasul de alocare + CUT).

“Lungimi solicitate” se formatează din requested_lengths (ex.: “50+20+30”).

Greutatea liniilor se preia din CSV (dacă există) pentru total.

Ce urmează (propunere)
Alocare/rezervări (MIN_WASTE/FIFO) și popularea automată a “din care se taie” + “rămasă”.
UI în Web UI:
Pagină “Comenzi” cu:
buton “Import CSV” (drag&drop),
listă comenzi + acțiune “Printează Nota de culegere”.
Retuș PDF:
adăugare logo, borduri mai groase, lățimi dinamice; opțional semnătură preluare și spațiu observații.
Vrei să continui direct cu:

buton “Import CSV” + listă comenzi + “Printă PDF” în frontend
sau
logica de alocare automată (și completare câmpuri “din care se taie” / “rămasă”)?